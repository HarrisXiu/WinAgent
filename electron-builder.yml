appId: com.winagent.app
productName: WinAgent
directories:
  output: release
  buildResources: build
files:
  - out/**/*
  - package.json
  # dsh-winagent 服务层（file: 依赖，symlink 解引用后打包；assets/skills 随包播种到 userData）
  - node_modules/dsh-winagent/**/*
win:
  target:
    - target: portable
      arch:
        - x64
  icon: build/icon.ico
  artifactName: WinAgent-${version}-win64.exe
compression: maximum
npmRebuild: false
